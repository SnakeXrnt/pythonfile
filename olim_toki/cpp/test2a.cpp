#include <bits/stdc++.h>
using namespace std;

int T, N;

int main() {
	cin >> T;
	
	for (int i = 1; i <= T; i++) {
		cin >> N;
		
		if (N % 2 == 0) cout << N / 2 - 1 << "\n";
		else cout << N / 2 << "\n"
	}
}

