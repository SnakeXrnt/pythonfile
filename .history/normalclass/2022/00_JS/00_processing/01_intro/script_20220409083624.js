function setup() {
    createCanvas(windowWidth, windowHeight);
    background(121);
    fill(255,0,0);
    noStroke();
    ellipse(width/2, height/2, 100, 100);
}

function draw() {
    //background(121);
}